<% provide(:title, @user.name) %>
<div class="row" >
  <!-- <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>

        <br>Username: <%= @user.name %></br>
        <br>Email: <%= @user.email %></br>
        <br>Password Digest: <%= @user.password_digest %></br>
        <br>Date Created: <%= @user.created_at %></br>
      </h1>
    </section>
  </aside> -->
  <!-- <%= gravatar_for @user %> --> 
  <table class="table table-striped">
    <thead>
      <tr>
        <th>User_Information:</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
     <tr>
        <td>  <div style="max-width:200px">
                  <%= image_tag @user.avatar.url %>        
              </div>
      </td>
     </tr>
      <tr>
        <td>Username:</td>
        <td><%= @user.name %></td>
      </tr>
      <tr>
        <td>Email:</td>
        <td><%= @user.email %></td>
      </tr>
      <tr>
        <td>Password Digest:</td>
        <td><%= @user.password_digest %></td>
      </tr>
      <tr>
        <td>Date Created:</td>
        <td><%= @user.created_at %></td>
      </tr>
    </tbody>
  </table>
</div>