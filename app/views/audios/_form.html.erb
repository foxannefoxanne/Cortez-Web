<%= form_for([@map, @pin, @audio]) do |f| %>
  <% if @audio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@audio.errors.count, "error") %> prohibited this audio from being saved:</h2>

      <ul>
      <% @audio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


     <%= audio_tag @audio.aud_clip.url, class: "audio-play" %>
           <p class="btn btn-primary audioButton">Play</p>
           <script>
              $(".audioButton").on("click", function() {
               $(".audio-play")[0].currentTime = 0;
               return $(".audio-play")[0].play();
             });
           </script>

  <div class="actions">
    <%= f.submit action_string, class: "btn btn-primary"  %>
  </div>
<% end %>
