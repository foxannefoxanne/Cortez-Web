<%
	map_hash =  {}

	map_arr = @maps.map{|entry| 
    	{
    		'mapName' => entry.title,
        	'mapDescription' => entry.description,
        	'mapLink' =>  "https://thawing-dusk-70157.herokuapp.com/maps/#{entry.id}/map_dump"
      	} 
    }

    map_hash['maps'] = map_arr

  %>
<pre><%= JSON.pretty_generate(map_hash) %></pre>

