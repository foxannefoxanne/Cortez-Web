<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @pin.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>

<p>
  <strong>Address:</strong>
  <%= @pin.address %>
</p>

<p>
  <strong>Latitude:</strong>
  <%= @pin.latitude %>
</p>

<p>
  <strong>Longitude:</strong>
  <%= @pin.latitude %>
</p>

<p>
  <strong>Information:</strong>
  <%= @pin.info %>
</p>


<% unless @pictures.empty? %>
 <p> <b> ATTACHED IMAGES </b> </p>
 <ul style="list-style-type: none";> 
  <% @pictures.each do |pic| %> 
     <li class="span3" id="picture_<%= pic.id %>">
        <div style="max-width:520px" class="thumbnail">
           <%= image_tag pic.image.url %>
        </div>
     </li>
  <% end %> 
  </ul>
<% end %> 

<% unless @audios.empty? %>
 <p> <b> ATTACHED AUDIO </b> </p>
 <ul style="list-style-type: none";> 
  <% @audios.each do |clip| %> 
     <li class="span3" id="audio_<%= clip.id %>">
        <div class="thumbnail">
           <%= audio_tag clip.aud_clip.url, class: "audio-play" %>
           <p class="btn btn-primary audioButton">Play</p>
           <script>
              $(".audioButton").on("click", function() {
               $(".audio-play")[0].currentTime = 0;
               return $(".audio-play")[0].play();
             });
           </script>
        </div>
     </li>
  <% end %> 
  </ul>
<% end %> 


<% unless @videos.empty? %>
 <p> <b> ATTACHED VIDEO </b> </p>

 <ul style="list-style-type: none";> 
  <% @videos.each do |clip| %> 
     <li class="span3" id="video_<%= clip.id %>">
           <video width="520" controls>
           <source src= "<%= clip.vid_clip.url %>" />
           Your browswer does not support the video tag 
           </video>
     </li>
  <% end %> 
    </ul>
<% end %> 

<%= link_to "Edit Media", map_pin_editmedia_path(:pin_id => @pin.id, :map_id => @map.id), class: "btn btn-primary" %>
<%= link_to 'Edit', edit_map_pin_path(:id => @pin.id, :map_id => @map.id),  class: "btn btn-primary" %>
<%= link_to 'Back', map_pins_path, class: "btn btn-primary", 'data-no-turbolink' => true   %>
